procs:
  client:
    cwd: ./client
    shell: bun run dev
    autostart: true

  admin:
    cwd: ./admin
    shell: bun run dev -- --port 3001
    autostart: true

  partykit:
    cwd: ./client
    shell: bun run dev:partykit
    autostart: true

  db-studio:
    cwd: ./database
    shell: bun run db:studio
    autostart: false
